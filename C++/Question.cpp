// ======= QUESTION PRACTICE====
#include <iostream>
using namespace std;
int main()
{
    // 1.	Two numbers are given, print their product
    // int a, b;
    // cout << "Enter the two number:";
    // cin >> a >> b;
    // cout << "Product of two number =" << (a * b) << endl;

    // // =========
    // // 2.Two numbers are given a and b, print a-b
    // int a, b;
    // cout << "Enter the two numbers :";
    // cin >> a >> b;
    // cout << "a - b = " << (a - b) << endl;

    // // ===========
    // // 3.	Print 6*3 in output, don’t print 18, just 6*3 should be in the output screen.
    // cout << "6*3" << endl; // prints text, not calculation

    // ========	If-else============
    // 1.Two numbers are given, print the bigger number, It is given that both numbers can’t be the same.

    return 0;
}
